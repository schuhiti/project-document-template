# AGENTS.md

本ファイルを正本とする（ツール固有のファイルをそれぞれ正本にすると、内容の二重管理が発生するため）。Claude Code用には`@AGENTS.md`をインポートするだけの`CLAUDE.md`を用意している。シンボリックリンクにしなかったのはWindowsでの管理者権限要件のため（同じ制約は`docs/system.md`の4を参照）。

前提: `docs/premise.md` が存在しない場合、先に `SETUP.md` を実行してから以降の内容に従う。

ドキュメント運用のルール（セッション開始時の手順・文書の参照先・文書化の原則）は `docs/system.md` を参照し、常に従う。

<!-- ここにこのプロジェクト固有の指示（ビルド・テスト手順、コーディング規約等）を追記する -->
