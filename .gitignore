# ビルド成果物（.dev/build-template.sh で再生成できる）
/project-template/
/project-template.zip

# Claude Codeが生成する作業ディレクトリ・個人用ローカル設定（配布不要）
/.claude/worktrees
/.claude/settings.local.json

# /.claude/skills はここに置かない: Linux/macOSでは真のシンボリックリンクとしてコミットする
# 対象のため。Windowsでの除外は各自の.git/info/excludeで行う（AGENTS.md「必ず行うこと」4）
